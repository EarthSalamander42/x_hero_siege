if XHSBossTelegraphs == nil then
	XHSBossTelegraphs = {}
end

XHSBossTelegraphs.PARTICLE = "particles/custom/xhs_boss_warning_circle.vpcf"
XHSBossTelegraphs.PARTICLE_FAMILIES = {
	arthas = {
		release = "particles/custom/boss_warnings/arthas/release.vpcf",
		release_contract = "release_radius",
		circle = "particles/custom/boss_warnings/arthas/radius.vpcf",
		target = "particles/custom/boss_warnings/arthas/target.vpcf",
		ring = "particles/custom/boss_warnings/arthas/radius.vpcf",
		line = "particles/custom/boss_warnings/arthas/line.vpcf",
		line_contract = "kez_line_duration_width",
		contract = "aghanim_radius_time",
	},
	balanar = {
		release = "particles/custom/boss_warnings/balanar/release.vpcf",
		release_contract = "release_radius",
		circle = "particles/custom/boss_warnings/balanar/radius.vpcf",
		target = "particles/custom/boss_warnings/balanar/target.vpcf",
		ring = "particles/custom/boss_warnings/balanar/radius.vpcf",
		line = "particles/custom/boss_warnings/balanar/line.vpcf",
		line_contract = "kez_line_duration_width",
		contract = "aghanim_radius_time",
	},
	grom = {
		release = "particles/custom/boss_warnings/grom/release.vpcf",
		release_contract = "release_radius",
		circle = "particles/custom/boss_warnings/grom/radius.vpcf",
		target = "particles/custom/boss_warnings/grom/target.vpcf",
		ring = "particles/custom/boss_warnings/grom/radius.vpcf",
		line = "particles/custom/boss_warnings/grom/line.vpcf",
		line_contract = "kez_line_duration_width",
		contract = "aghanim_radius_time",
	},
	illidan = {
		release = "particles/custom/boss_warnings/illidan/release.vpcf",
		release_contract = "release_radius",
		circle = "particles/custom/boss_warnings/illidan/radius.vpcf",
		target = "particles/custom/boss_warnings/illidan/target.vpcf",
		ring = "particles/custom/boss_warnings/illidan/radius.vpcf",
		line = "particles/custom/boss_warnings/illidan/line.vpcf",
		line_contract = "kez_line_duration_width",
		contract = "aghanim_radius_time",
	},
	lich_king = {
		release = "particles/custom/boss_warnings/lich_king/release.vpcf",
		release_contract = "release_radius",
		circle = "particles/custom/boss_warnings/lich_king/radius.vpcf",
		target = "particles/custom/boss_warnings/lich_king/target.vpcf",
		ring = "particles/custom/boss_warnings/lich_king/radius.vpcf",
		line = "particles/custom/boss_warnings/lich_king/line.vpcf",
		line_contract = "kez_line_duration_width",
		contract = "aghanim_radius_time",
	},
	magtheridon = {
		release = "particles/custom/boss_warnings/magtheridon/release.vpcf",
		release_contract = "release_radius",
		circle = "particles/custom/boss_warnings/magtheridon/radius.vpcf",
		target = "particles/custom/boss_warnings/magtheridon/target.vpcf",
		ring = "particles/custom/boss_warnings/magtheridon/radius.vpcf",
		line = "particles/custom/boss_warnings/magtheridon/line.vpcf",
		line_contract = "kez_line_duration_width",
		contract = "aghanim_radius_time",
	},
	proudmoore = {
		release = "particles/custom/boss_warnings/proudmoore/release.vpcf",
		release_contract = "release_radius",
		circle = "particles/custom/boss_warnings/proudmoore/radius.vpcf",
		target = "particles/custom/boss_warnings/proudmoore/target.vpcf",
		ring = "particles/custom/boss_warnings/proudmoore/radius.vpcf",
		line = "particles/custom/boss_warnings/proudmoore/line.vpcf",
		line_contract = "kez_line_duration_width",
		contract = "aghanim_radius_time",
	},
	special = {
		release = "particles/custom/boss_warnings/special/release.vpcf",
		release_contract = "release_radius",
		circle = "particles/custom/boss_warnings/special/radius.vpcf",
		target = "particles/custom/boss_warnings/special/target.vpcf",
		ring = "particles/custom/boss_warnings/special/radius.vpcf",
		line = "particles/custom/boss_warnings/special/line.vpcf",
		line_contract = "kez_line_duration_width",
		contract = "aghanim_radius_time",
	},
	spirit_master = {
		release = "particles/custom/boss_warnings/spirit_master/release.vpcf",
		release_contract = "release_radius",
		circle = "particles/custom/boss_warnings/spirit_master/radius.vpcf",
		target = "particles/custom/boss_warnings/spirit_master/target.vpcf",
		ring = "particles/custom/boss_warnings/spirit_master/radius.vpcf",
		line = "particles/custom/boss_warnings/spirit_master/line.vpcf",
		line_contract = "kez_line_duration_width",
		contract = "aghanim_radius_time",
	},
	spirit_storm = {
		release = "particles/custom/boss_warnings/spirit_storm/release.vpcf",
		release_contract = "release_radius",
		circle = "particles/custom/boss_warnings/spirit_storm/radius.vpcf",
		target = "particles/custom/boss_warnings/spirit_storm/target.vpcf",
		ring = "particles/custom/boss_warnings/spirit_storm/radius.vpcf",
		line = "particles/custom/boss_warnings/spirit_storm/line.vpcf",
		line_contract = "kez_line_duration_width",
		contract = "aghanim_radius_time",
	},
	spirit_earth = {
		release = "particles/custom/boss_warnings/spirit_earth/release.vpcf",
		release_contract = "release_radius",
		circle = "particles/custom/boss_warnings/spirit_earth/radius.vpcf",
		target = "particles/custom/boss_warnings/spirit_earth/target.vpcf",
		ring = "particles/custom/boss_warnings/spirit_earth/radius.vpcf",
		line = "particles/custom/boss_warnings/spirit_earth/line.vpcf",
		line_contract = "kez_line_duration_width",
		contract = "aghanim_radius_time",
	},
	spirit_fire = {
		release = "particles/custom/boss_warnings/spirit_fire/release.vpcf",
		release_contract = "release_radius",
		circle = "particles/custom/boss_warnings/spirit_fire/radius.vpcf",
		target = "particles/custom/boss_warnings/spirit_fire/target.vpcf",
		ring = "particles/custom/boss_warnings/spirit_fire/radius.vpcf",
		line = "particles/custom/boss_warnings/spirit_fire/line.vpcf",
		line_contract = "kez_line_duration_width",
		contract = "aghanim_radius_time",
	},
}
XHSBossTelegraphs.DEFAULT_PRIMARY = Vector(255, 110, 35)
XHSBossTelegraphs.DEFAULT_SECONDARY = Vector(120, 255, 80)

local function ValidVector(value)
	return value ~= nil and value.x ~= nil and value.y ~= nil and value.z ~= nil
end

local function ResolveParticle(colors, geometry)
	local family = colors and colors.family and XHSBossTelegraphs.PARTICLE_FAMILIES[colors.family]
	if family == nil then
		return XHSBossTelegraphs.PARTICLE, "xhs_generic"
	end
	return family[geometry] or family.circle or XHSBossTelegraphs.PARTICLE, family[geometry .. "_contract"] or family.contract
end

local function RegisterCircleDanger(position, radius, duration)
	if XHSBotDangerRegistry ~= nil and XHSBotDangerRegistry.AddCircle ~= nil then
		XHSBotDangerRegistry:AddCircle(position, radius or 180, duration or 1.0, {
			severity = 1,
			label = "phase3_boss_telegraph",
		})
	end
end

local function CreateWarning(position, radius, duration, colors, geometry, skipDangerRegistry)
	if not ValidVector(position) then return nil end
	colors = colors or {}
	local primary = colors.primary or XHSBossTelegraphs.DEFAULT_PRIMARY
	local secondary = colors.secondary or XHSBossTelegraphs.DEFAULT_SECONDARY
	local style = colors.style or 0

	-- The visual telegraph remains authoritative for players. In a Tools bot
	-- session, mirror it into the AI danger registry so bots react to the exact
	-- same warning window instead of reading hidden spell state.
	if skipDangerRegistry ~= true then RegisterCircleDanger(position, radius, duration) end

	local particlePath, contract = ResolveParticle(colors, geometry)
	local particle = ParticleManager:CreateParticle(particlePath, PATTACH_WORLDORIGIN, nil)
	ParticleManager:SetParticleControl(particle, 0, position)
	if contract == "aghanim_radius_time" then
		-- Aghanim telegraphs use CP1 = (world radius, lifetime, reserved),
		-- CP15 as the authored tint and CP16.x as tint influence.
		ParticleManager:SetParticleControl(particle, 1, Vector(radius or 180, duration or 1.0, 0))
		ParticleManager:SetParticleControl(particle, 15, primary)
		ParticleManager:SetParticleControl(particle, 16, Vector(1, 0, 0))
	else
		ParticleManager:SetParticleControl(particle, 1, Vector(radius or 180, 0, 0))
	end
	ParticleManager:SetParticleControl(particle, 2, Vector(duration or 1.0, 0, 0))
	ParticleManager:SetParticleControl(particle, 3, primary)
	ParticleManager:SetParticleControl(particle, 4, secondary)
	ParticleManager:SetParticleControl(particle, 5, Vector(style, 0, 0))
	Timers:CreateTimer(math.max(0.1, duration or 1.0) + 0.15, function()
		ParticleManager:DestroyParticle(particle, false)
		ParticleManager:ReleaseParticleIndex(particle)
		return nil
	end)
	return particle
end

local function CreateLineWarning(startPosition, endPosition, width, duration, colors)
	local particlePath, contract = ResolveParticle(colors, "line")
	if contract ~= "kez_line_duration_width" then return nil end

	colors = colors or {}
	local primary = colors.primary or XHSBossTelegraphs.DEFAULT_PRIMARY
	local secondary = colors.secondary or XHSBossTelegraphs.DEFAULT_SECONDARY
	local particle = ParticleManager:CreateParticle(particlePath, PATTACH_WORLDORIGIN, nil)
	ParticleManager:SetParticleControl(particle, 0, startPosition)
	ParticleManager:SetParticleControl(particle, 1, endPosition)
	ParticleManager:SetParticleControl(particle, 2, Vector(duration or 1.0, width or 180, 1))
	ParticleManager:SetParticleControl(particle, 15, primary)
	ParticleManager:SetParticleControl(particle, 16, secondary)
	Timers:CreateTimer(math.max(0.1, duration or 1.0) + 0.15, function()
		ParticleManager:DestroyParticle(particle, false)
		ParticleManager:ReleaseParticleIndex(particle)
		return nil
	end)
	return particle
end

function XHSBossTelegraphs:Circle(position, radius, duration, colors)
	return CreateWarning(position, radius, duration, colors, "circle")
end

function XHSBossTelegraphs:Target(position, radius, duration, colors)
	return CreateWarning(position, radius, duration, colors, "target")
end

-- Visual payoff only: CP0 is the cast origin and CP1.x is the final radius.
-- It deliberately does not register danger or own any gameplay timing.
function XHSBossTelegraphs:Release(position, radius, colors)
	if not ValidVector(position) then return nil end
	local particlePath, contract = ResolveParticle(colors or {}, "release")
	if contract ~= "release_radius" then return nil end
	local particle = ParticleManager:CreateParticle(particlePath, PATTACH_WORLDORIGIN, nil)
	ParticleManager:SetParticleControl(particle, 0, position)
	ParticleManager:SetParticleControl(particle, 1, Vector(radius or 180, 0, 0))
	Timers:CreateTimer(0.5, function()
		ParticleManager:DestroyParticle(particle, false)
		ParticleManager:ReleaseParticleIndex(particle)
	end)
	return particle
end

function XHSBossTelegraphs:Ring(center, ringRadius, nodeRadius, count, duration, colors, offsetDegrees)
	if not ValidVector(center) then return end

	count = math.max(1, count or 8)
	for i = 1, count do
		local angle = ((i - 1) / count) * 360 + (offsetDegrees or 0)
		local position = RotatePosition(center, QAngle(0, angle, 0), center + Vector(ringRadius or 500, 0, 0))
		CreateWarning(position, nodeRadius or 180, duration, colors, "ring")
	end
end

function XHSBossTelegraphs:Line(startPosition, direction, spacing, nodeRadius, count, duration, colors, startDistance)
	if not ValidVector(startPosition) or direction == nil then return end

	direction.z = 0
	if direction:Length2D() <= 0 then direction = Vector(1, 0, 0) end
	direction = direction:Normalized()
	count = math.max(1, count or 1)

	local positions = {}
	for i = 1, count do
		local position = startPosition + direction * ((startDistance or 0) + (spacing or 0) * (i - 1))
		positions[i] = position
		RegisterCircleDanger(position, nodeRadius or 180, duration)
	end

	if count > 1 and CreateLineWarning(positions[1], positions[count], nodeRadius, duration, colors) ~= nil then
		return
	end

	for _, position in ipairs(positions) do
		CreateWarning(position, nodeRadius or 180, duration, colors, "circle", true)
	end
end
